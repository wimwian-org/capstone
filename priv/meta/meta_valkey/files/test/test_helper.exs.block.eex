
# Redix has no official test/stub tooling, so <%= @module %>.ValkeyTest exercises
# the real sidecar and is excluded by default -- opt in with
# `mix test --include valkey`.
ExUnit.configure(exclude: [valkey: true])
