# Dialyzer warnings deliberately suppressed, each with a reason.
#
# Entries are `{file}` / `{file, warning}` / `{file, warning, line}` tuples or
# regexes. `list_unused_filters: true` in mix.exs makes a filter that no longer
# matches fail the run, so this file cannot quietly accumulate dead entries.
[]
